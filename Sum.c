#include <stdio.h>
int main()
{

    // a program that can add two numbers and returns a sum
    int a = 4;
    int b = 7;
    int c;
    c = a + b;

    

    printf("the sum is %d", c); // the % sign is for allowing a string formatter/ placeholder(position of varible) and then put the value of the sum
    
    //attempt the user to input the valeus
    int number1;
    int number3;
    int number2;
     printf('Enter the first number `"with"`:>');
     scanf("%d, &number1, &number2, &number3");

    return (0);
}
